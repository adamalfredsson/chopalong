import React from 'react';
import { Styled{{ pascalCase name }} } from './style';

export const {{ pascalCase name }}: React.FC = ({ children, ...props }) => {
  return (
    <Styled{{ pascalCase name }}>
      <p>New component!</p>
    </Styled{{ pascalCase name }}>
  );
};
